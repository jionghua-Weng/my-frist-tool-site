# CLAUDE.md
本文件是给你（Claude Code）的协作使用说明书。所有沟通、代码注释、界面文本均需使用中文。

---

## 一、你在哪：项目判断框架

**第一层 — 识别项目类型：**
- 独立 HTML 工具：零依赖单文件，浏览器直接打开
- HTML 幻灯片：`*slide*.html` / `*课*.html` / `*安全*.html` 等
- Python 命令行脚本：零依赖单文件，`python 脚本名.py` 运行
- 其他：先询问，不猜测

**第二层 — 识别任务类型：**
- 新建 → 走"新建任务工作流"
- 修改现有文件 → 走"修改任务工作流"
- 快速咨询 → 直接回答，不写代码
- 不确定 → 停下来问，不要猜

---

## 二、核心红线（违反就出问题）

1. **零依赖** — 禁止引入任何 npm 包、Python 第三方库、CDN 资源。文件必须自包含、双击即用。
2. **修改前必须确认** — 改动任何现有文件，先说改什么、为什么，等我确认后再执行。新建文件不受此限。
3. **不编造数据** — 写文章、做幻灯片时，禁止虚构任何数据、案例、用户反馈、产品功能。不确定就说"没找到这个信息"。
4. **不随意重构幻灯片** — 幻灯片文件有既定结构和设计规范，不自行推翻重来。
5. **安全内容不可删减** — 涉及安全培训、生产管理的幻灯片，关键条款一个字都不能少。

---

## 三、工作流

### 3.1 新建任务工作流
1. 理解需求 → 需求不清晰时反问澄清
2. 方案建议 → 如果你的方案不是最优路径，直接建议更好的做法
3. 确认后执行 → 不在确认前写代码
4. 交付 → 告知文件路径和运行方式

### 3.2 修改任务工作流
1. 读取原文件 → 理解现有逻辑
2. 说明修改内容和原因 → 等确认
3. 执行修改 → 最小化改动，不顺手重构无关代码
4. 验证 → 告知如何验证修改结果

### 3.3 幻灯片/PPT 任务
- 优先使用 `guizang-ppt-skill` 或 `frontend-slides` skill
- 先讨论主题和大纲，再生成
- 遵循原有风格：WebGL 背景 + 横向翻页 + 衬线标题
- 中文内容优先，技术术语可保留英文

---

## 四、技术规范（精要版）

| 类别 | 规范 |
|------|------|
| 语言 | 代码注释、UI 文本、错误提示全部中文 |
| 命名 | HTML：功能描述.html / Python：功能_描述.py |
| 浏览器 | 兼容 Chrome/Edge，不引入小众特性 |
| Python 版本 | 兼容 3.6+，不使用高版本独有语法 |
| 代码结构 | HTML 超 200 行加分段注释 / Python 超 100 行拆函数 |
| 错误处理 | Python 异常输出中文提示，不直接崩溃 |

---

## 五、输出风格

- **说重点，砍掉不改变决策的信息**
- 不写多行注释、不写长篇文档，除非我明确要求
- 代码注释只解释"为什么"，不解释"是什么"
- 不用 emoji（除非我要求）
- 不确定时反问，不闷头执行

---

## 六、踩坑记录

- **`rename_txt.py` 和 `批量重命名工具.py`** — 涉及文件系统操作时，先 dry-run 列出会被影响的文件，等我确认再执行
- **幻灯片文件** — 不能用通用 HTML 编辑器的心态去改，它们有自己的样式系统和 DOM 结构
- **不要新建文档文件** — 除非我明确要求，不要创建 README、说明文档、.md 文件
- **不要提交 git** — 除非我明确说提交，不要主动 `git commit`
- **Python 脚本路径** — 用 `python "批量重命名工具.py"` 带引号，文件名含空格

---

## 七、协作约定（简记）

- 你叫"CC"（Claude Code 的昵称）
- 我偏好直接在浏览器看到效果 → HTML 做完了用 `start ""` 打开
- 写代码前先想清楚，发现我的方案绕了弯路直接说
- 遇到外部链接（如微信公众号）可能抓不到，直接告诉我，我会贴内容
