# CLAUDE.md — 英语研习社

本文件是你的协作说明书。代码注释、界面文本均用中文。

---

## 一、项目信息

- **域名**：`https://easyeng.club`
- **GitHub**：`git@github.com:jionghua-Weng/my-frist-tool-site.git`
- **AdSense**：`ca-pub-6082621192201664`
- **类型**：静态 HTML 网站，零依赖，浏览器直接打开

## 二、核心红线

1. **零依赖** — 不引入 npm 包、CDN 资源。文件自包含、双击即用。
2. **修改前必须确认** — 改现有文件，先说改什么、为什么，确认后再执行。新建文件不受此限。
3. **不编造数据** — 写文章、做内容时，不虚构任何数据、案例。不确定就说"没找到"。
4. **不新建文档** — 除非明确要求，不创建 README、说明文档等 .md 文件。
5. **不主动 git commit** — 除非明确要求提交。
6. **全站变动必须审计** — 导航栏、公共脚本、全局样式等批量修改（影响 >10 个文件），跑完后必须用脚本审计全站，确认所有文件一致。不依赖"看起来 OK"。

## 三、工作流

### 修改任务
1. 读取原文件 → 理解现有逻辑
2. 说明修改内容和原因 → 等确认
3. 执行修改 → 最小化改动，不顺手重构无关代码
4. 验证 → 告知如何验证结果

### 新建任务
1. 理解需求 → 不清晰时反问
2. 方案建议 → 不是最优路径直接说
3. 确认后执行
4. 交付 → 告知文件路径和运行方式

## 四、技术规范

| 类别 | 规范 |
|------|------|
| 语言 | 代码注释、UI 文本全部中文 |
| 浏览器 | 兼容 Chrome/Edge |
| 代码结构 | HTML 超 200 行加分段注释 |
| 文件命名 | 功能描述.html |

## 五、网站结构

**六大板块**：首页 | 语法精讲 | 词汇辨析 | 实用句型 | 场景口语 | 发音诊所 | 语法魔典(发光导航) | 关于

**语法魔典系列**（16篇+附录，已完结）：纯文字体系化语法，魔幻写作风格，定位初高中/零基础。后续只做维护性修改。

## 六、输出风格

- 说重点，砍掉不改变决策的信息
- 代码注释只解释"为什么"，不解释"是什么"
- 不写多行注释、不写长篇文档
- 不确定时反问，不闷头执行

## 七、协作约定

- 叫我"CC"
- HTML 做完用 `start "" 文件名` 打开浏览器查看
- 写代码前先想清楚，方案绕了弯路直接说
